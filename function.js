//get the user's hours worked X
//get user'a pay rate X
//if the worked overtime
	//calculate overtime pay
	//calculate pay for first 40 hrs
	//add together
//else
	//calculate "regular" pay
//total pay


// function reverseString(str) {
// 	let hello = ['h', 'e', 'l', 'l', 'o'] {
// 		for(let i = 0; i < 5; i++){
// 			if()
// 		console.log(reversed);
// 	}
	
//   	let h = Array[4];
//   	let e = Array[3];
// 		let l = Array[2];
// 		let l = Array[1];
// 		let o = Array[0];
// 		return hello; 
// }

// reverseString("hello");

// "hello"
// function reverse(hello) {

// 	let reverseString = ""; 

// 	for(let i = hello.length - 1; i >= 0; i--) {  //notes: instead of the actual number lenth for hello (4), *replace with hello.length minus 1
// 		reverseString += hello[i];	 //[i] leave as "i" so that it prints the value of each letter separately
 	// }
 		//below are practice ideas
 	// return reverseString;
	// let hello = ['h', 'e', 'l', 'l', 'o'] {
	// 	for(let i = 0; i < 5; i++)
	// }  
// }
// let test = reverse("hello");
// console.log(test);
//end of "hello" warm-up

// function upperCaseFirstLetter(hello, world) {
// 	let upperCaseFirstletter = "";
// 	return upperCaseFirstletter;
// 	for(let i = hello.length - 1 && let i = world.length - 1);
// 		if(i <= 0 && i <=0);
// 	console.log(upperCaseFirstletter)
// }
// console.log(result);


function upperCaseFirstLetter(stringParameter) {
	for(let i = 0; i <= 0; i--) {
		if(i <= hello.length && i <= world.length);
		return upperCaseFirstletter;
	}
console.log(upperCaseFirstletter);
}


